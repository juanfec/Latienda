<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amovil.latienda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".actividades.InitActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".actividades.SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".actividades.Login" />
        <activity
            android:name=".actividades.TusPedidos"
            android:theme="@style/Blanco" />
        <activity
            android:name=".actividades.Factura"
            android:theme="@style/Blanco" />
        <activity android:name=".actividades.Registro"
            android:theme="@style/Blanco"/>
        <activity android:name=".actividades.RecuperaTuContrasena"
            android:theme="@style/Blanco"></activity>
    </application>

</manifest>